<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- To be able to display contacts list & match calling/called numbers -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- fork: zrcadlení ukončených hovorů do systémové historie, aby je vidělo auto (PBAP) -->
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />

    <!-- fork: kontakty se ukládají do systémového adresáře — auto (PBAP)
         i systémové Kontakty pak vidí totéž -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- SMS do auta: role výchozí SMS aplikace — zrcadlení zpráv do
         systémového úložiště SMS, odkud si je auto čte přes Bluetooth -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />

    <!-- Starting Android 13 we need to ask notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Needed for full screen intent in incoming call notifications -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- To vibrate while receiving an incoming call -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Needed for foreground service
    (https://developer.android.com/guide/components/foreground-services) -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Needed for Android 14
    https://developer.android.com/about/versions/14/behavior-changes-14#fgs-types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <!-- Required for foreground service started when a push is being received,
    without it app won't be able to access network if data saver is ON (for example) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Needed to keep a permanent foreground service and keep app alive to be able to receive
    messages & calls for third party accounts for which push notifications aren't available,
    and starting Android 15 dataSync is limited to 6 hours per day
    and can't be used with RECEIVE_BOOT_COMPLETED intent either -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Required starting Android 17 to be able to connect to local network server -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!-- Needed for auto start at boot if keep alive service is enabled -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Zabudovaný tunel do privátní sítě -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Běh na pozadí bez omezení baterie (dialog si vyžádá aplikace sama) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Viditelnost synchronizační aplikace kontaktů (Android 11+) -->
    <queries>
        <package android:name="at.bitfire.davdroid" />
    </queries>

    <application
        android:name=".LinphoneApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:enableOnBackInvokedCallback="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Linphone"
        android:appCategory="social"
        android:largeHeap="true"
        tools:targetApi="35">

        <!-- Required for chat message & call notifications to be displayed in Android auto -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <!--<meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1"/>-->

        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppSplashScreenTheme"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:launchMode="singleTask">

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW_LOCUS" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
                <data android:mimeType="image/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="application/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="application/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.CALL" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tel" />
                <data android:scheme="sip" />
                <data android:scheme="callto" />
                <data android:scheme="sips" />
                <data android:scheme="sip-linphone" />
                <data android:scheme="sips-linphone" />
                <data android:scheme="linphone-sip" />
                <data android:scheme="linphone-sips" />
                <data android:scheme="linphone-config" />
            </intent-filter>

        </activity>

        <!-- SMS do auta: příjem odkazů sms:/smsto: (povinná součást role SMS).
             MainActivity na SENDTO nereaguje — aplikace se jen otevře. -->
        <activity-alias
            android:name="org.linphone.twentyone.car.CarSmsSendToAlias"
            android:targetActivity=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ui.welcome.WelcomeActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.assistant.AssistantActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.sso.SingleSignOnActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.fileviewer.MediaViewerActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.fileviewer.FileViewerActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.call.CallActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LinphoneInCall"
            android:launchMode="singleInstance"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true" />

        <!-- Services -->

        <service
            android:name=".core.CoreInCallService"
            android:exported="false"
            android:foregroundServiceType="phoneCall|camera|microphone"
            android:stopWithTask="false"
            android:label="@string/app_name" />

        <service
            android:name=".core.CorePushService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            android:label="@string/app_name" />

        <service
            android:name=".core.CoreFileTransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            android:label="@string/app_name" />

        <activity
            android:name="org.linphone.twentyone.vpn.TsConsentActivity"
            android:exported="false"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@style/TwentyOneTranslucent" />

        <service
            android:name="org.linphone.twentyone.vpn.TsVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false"
            android:label="@string/app_name">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
            <!-- Tunel neumí být jediná síť telefonu — systémové always-on
                 (a s ním „Blokovat připojení bez VPN") se nenabízí. -->
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="false" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- fork: vytáčení z auta — účet pro volání (managed telecom).
             Dokud volba v aplikaci nezaregistruje PhoneAccount, systém
             se na službu nikdy nenaváže. -->
        <service
            android:name="org.linphone.twentyone.car.CarConnectionService"
            android:exported="true"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

        <service android:name="org.linphone.core.tools.firebase.FirebaseMessaging"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".core.CoreKeepAliveThirdPartyAccountsService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false"
            android:label="@string/app_name">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Needed to keep app alive to be able to receive messages and calls from third party SIP servers for which push notifications aren't available." />
        </service>

        <!-- SMS do auta: povinná služba role SMS („odmítnout hovor zprávou") -->
        <service
            android:name="org.linphone.twentyone.car.CarRespondService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!--<service
            android:name=".telecom.auto.AndroidAutoService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService"/>
                <category android:name="androidx.car.app.category.CALLING"/>
            </intent-filter>
        </service>-->

        <!-- Receivers -->

        <receiver android:name=".core.CorePushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.linphone.core.action.PUSH_RECEIVED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notifications.NotificationBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- SMS do auta: povinné přijímače role SMS. exported=true je nutné,
             ale oprávnění BROADCAST_SMS/BROADCAST_WAP_PUSH drží jen systém. -->
        <receiver
            android:name="org.linphone.twentyone.car.CarSmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="org.linphone.twentyone.car.CarWapPushReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <receiver android:name=".core.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Providers -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/file_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>